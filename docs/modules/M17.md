# M17 — Demo dApp (Next.js)

**Data:** 2026-05-06  
**Status:** Parcial  

---

## O que entrou

- scaffold Next.js em `app/`
- App Router com:
  - `app/app/layout.tsx`
  - `app/app/page.tsx`
  - `app/app/api/sign-register/route.ts`
- UI inicial com 3 superfícies:
  - `ConnectCard`
  - `ProfilePanel`
  - `ProposalsPanel`
  - `TreasuryPanel`
- integração com M16 via:
  - `KarnSolanaProvider`
  - `useValocracy`
  - `useGovernor`
  - `useTreasury`
- wallet browser-side simples em `app/lib/browser-wallet.ts`
- bootstrap de `Program`s Anchor em `app/lib/programs.ts`
- `.env.example` para RPC e signing key backend
- build de produção validada com `next build`

## O que a demo cobre

- conectar wallet de navegador
- chamar `/api/sign-register`
- submeter `self_register`
- ler `UserStats` e Mana
- criar proposta
- votar
- executar proposta
- fundar lab
- sacar scholarship

## O que ainda falta para considerar M17 concluído

- subir `next dev` e validar runtime real
- testar integração com Phantom/Backpack/Solflare em devnet
- revisar fluxo de proposal execution com ATAs reais para variantes que exigem `remaining_accounts`
- opcionalmente adicionar uma tela/detail view separada para propostas e labs

## Motivo do status parcial

O código do app foi scaffoldado, integrado ao SDK e validado em build de produção, mas ainda não foi exercitado end-to-end com wallet real e devnet neste checkout.
