# M12 — Lab/Scholarship Escrow (KRN-01)

**Data:** 2026-05-01  
**Status:** ✅ Concluído  
**Testes:** 6/6 passando (60 total no suite)

---

## Objetivo

Implementar o fluxo completo de bolsas de estudo: Funder cria Lab, Governor aprova bolsa para membro, membro retira os tokens. Todos os fundos de bolsa ficam em `restricted_reserves`, garantindo que `total_assets()` reflita apenas os ativos disponíveis para governança (KRN-01).

---

## Arquivos Criados / Modificados

### `programs/treasury/src/state.rs` (atualizado)

**`TreasuryState`** — Campo `lab_counter: u32` adicionado (SIZE: 122 → 126 bytes):
```rust
pub lab_counter: u32,  // incrementado a cada fund_lab
```

**`LabStatus`** — Enum Borsh-serializado:
```rust
pub enum LabStatus { Active, Cancelled, Completed }
```

**`Lab`** — PDA por lab_id (SIZE=54):
```rust
pub struct Lab {
    pub id: u32,
    pub funder: Pubkey,
    pub total_amount: u64,
    pub scholarship_per_member: u64,
    pub status: LabStatus,
    pub bump: u8,
}
// Seeds: [b"lab", id.to_le_bytes()]
```

**`Claimable`** — PDA por member (SIZE=41):
```rust
pub struct Claimable {
    pub member: Pubkey,
    pub amount: u64,
    pub bump: u8,
}
// Seeds: [b"claimable", member.as_ref()]
```

### `programs/treasury/src/instructions/fund_lab.rs` (novo)

Fluxo:
1. Validações: `total_amount > 0`, `scholarship_per_member > 0`, `scholarship_per_member <= total_amount`
2. CPI: `spl_token::transfer(funder_ata → vault_ata, total_amount)` — funder assina
3. Init `Lab` PDA com `id = state.lab_counter`, status=Active
4. `state.restricted_reserves += total_amount`
5. `state.lab_counter += 1`
6. Emite `LabFunded { lab_id, funder, total_amount }`

### `programs/treasury/src/instructions/approve_scholarship.rs` (novo)

- `#[instruction(lab_id: u32)]` para derivar Lab PDA nos seeds
- Constraint: `lab.status == LabStatus::Active @ LabNotActive`
- `has_one = governor @ NotAuthorized` no state
- `init_if_needed` no `Claimable` PDA (payer = governor)
- `claimable.amount += lab.scholarship_per_member` (saturating)
- Emite `ScholarshipReleased { lab_id, member, amount }`

### `programs/treasury/src/instructions/withdraw_scholarship.rs` (novo)

- Constraint: `claimable.member == member.key() @ NotAuthorized`
- Guards: `amount > 0`, `claimable.amount >= amount`
- `claimable.amount -= amount`
- `state.restricted_reserves = restricted_reserves.saturating_sub(amount)`
- CPI: `spl_token::transfer(vault → member_ata, amount)` com seeds `[b"treasury", &[bump]]`
- Emite `ScholarshipWithdrawn { member, amount }`

### `programs/treasury/src/instructions/initialize.rs` (atualizado)

Adicionado `state.lab_counter = 0`.

---

## Invariante KRN-01 Verificado

Em qualquer ponto:
```
total_assets = vault_balance - restricted_reserves
```

- Tokens que entram via `fund_lab` sempre aumentam `vault_balance` E `restricted_reserves` pelo mesmo valor → `total_assets` inalterado
- `withdraw_scholarship` diminui `vault_balance` E `restricted_reserves` pelo mesmo valor → `total_assets` inalterado
- `approve_scholarship` não move tokens → `total_assets` inalterado

Portanto, a quantidade acessível pela governança (`total_assets`) é SEMPRE igual aos tokens "livres" no vault, independente de quantos labs estejam ativos ou quantas bolsas estejam aprovadas.

---

## Testes (Bankrun)

| # | Teste | Resultado |
|---|-------|-----------|
| 1 | `fund_lab` cria Lab PDA e incrementa restricted_reserves | ✅ |
| 2 | `approve_scholarship` cria Claimable para membro | ✅ |
| 3 | `withdraw_scholarship` transfere tokens e decrementa restricted | ✅ |
| 4 | KRN-01: total_assets = vault_balance − restricted_reserves constante | ✅ |
| 5 | Withdraw acima do claimable falha (InsufficientClaimable) | ✅ |
| 6 | Double-withdraw falha após esgotamento (InsufficientClaimable) | ✅ |

---

## Próximo: M13 — Programa Governor: Proposals + Config
