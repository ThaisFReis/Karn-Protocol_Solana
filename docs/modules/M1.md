# M1 — Workspace Anchor + Tooling

**Data:** 2026-04-30
**Status:** Concluído
**PRD:** `~/Documentos/Workspace/Karn Protocol/docs/solana/PRD.md` §3 Módulo 1
**Owner:** Solo developer

## Escopo entregue

- Workspace Anchor inicializado em `~/Documentos/Workspace/Karn-Protocol-Solana/`
- 3 programas scaffoldados em `programs/`:
  - `valocracy` (`6WEzighM5X9pCbwLpbnC3SHc8E92YtNcH7SsBDksLHgf`)
  - `governor` (`6RfCxo65k9KZaJZvpHDEaav1ahDcx7hn13XBdmDtdLRm`)
  - `treasury` (`97LKXR8q7yg8GmQAYQzpZNLnttyaHbZhR61q6ANw3dbV`)
- Crate compartilhada `karn-shared` em `crates/karn-shared/` (movida de `programs/` durante o módulo — ver ADR-0001)
- `Anchor.toml` com `[programs.localnet]` e `[programs.devnet]` apontando pros 3 IDs
- `Cargo.toml` workspace com `members = ["programs/*", "crates/*"]`
- Cada program com `anchor-spl = "0.32.1"` e `karn-shared = { path = "../../crates/karn-shared" }`
- `karn-shared` com feature `idl-build` para satisfazer Anchor IDL pipeline
- CI em `.github/workflows/ci.yml` (fmt + clippy + anchor build + anchor test)
- `package.json` com scripts `build`, `test`, `deploy:devnet`
- `.gitignore` estendido (.env, IDE, deployments/)
- `README.md` com overview, programas, stack, quick start
- `CONFIG.md` (fonte de verdade para IAs) e `CLAUDE.md` (pointer)
- `docs/` estrutura inicial com README, templates e entradas retroativas deste módulo

## Critério de aceite (do PRD)

- [x] `anchor build && anchor test` retorna 0 sem warnings críticos
- [x] IDLs JSON gerados em `target/idl/` (`valocracy.json`, `governor.json`, `treasury.json`)

## Decisões tomadas

- [ADR-0001 — `karn-shared` em `crates/`, não em `programs/`](../decisions/0001-karn-shared-location.md)

## Erros encontrados

- [karn-shared IDL extraction failure](../errors/karn-shared-idl-extraction.md) — Anchor tentou extrair IDL de `karn-shared` por estar em `programs/*`; resolvido movendo para `crates/`.

## Comandos de verificação

```bash
cd ~/Documentos/Workspace/Karn-Protocol-Solana
anchor build
ls target/idl/
# valocracy.json governor.json treasury.json
```

## Caveats / dívidas técnicas

- `tests/` está vazia. Testes serão adicionados em M2 (smoke) e nos módulos seguintes (lógica + KRN).
- `migrations/deploy.ts` é o stub do `anchor init` — será reescrita em M18.
- `app/` é stub do `anchor init`; será descartada se M17 (demo dApp) ficar como stretch.
- Upgrade authority dos programas é a chave do dev (não Governor PDA) — ver DT-12 do PRD.
- `anchor test` no CI está com `|| echo "tests not yet wired"` — remover quando M2 adicionar primeiros testes.

## Próximo

- M2 — Convenções de PDA, Erros e Eventos. PRD §3 Módulo 2.
